[workspace]
resolver = "2"
members = [
    "crates/polyglot-sql",
    "crates/polyglot-sql-ast-derive",
    "crates/polyglot-sql-function-catalogs",
    "crates/polyglot-sql-wasm",
    "crates/polyglot-sql-ffi",
    "crates/polyglot-sql-python",
]
exclude = [
    "examples/rust",
]

[workspace.package]
version = "0.9.0"
edition = "2021"
license = "MIT"
authors = ["polyglot contributors"]
repository = "https://github.com/tobilg/polyglot"
homepage = "https://github.com/tobilg/polyglot"
documentation = "https://docs.rs/polyglot-sql"
description = "SQL dialect translator compiled to WebAssembly"
keywords = ["sql", "transpile", "dialect", "parser", "wasm"]
categories = ["compilers", "parser-implementations", "wasm"]

[workspace.dependencies]
# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# WASM support
wasm-bindgen = "0.2"
js-sys = "0.3"
web-sys = "0.3"

# Error handling
thiserror = "1.0"

# Utilities
unicode-segmentation = "1.10"

[profile.release]
opt-level = "z"     # Optimize for size
lto = true          # Link-time optimization
codegen-units = 1   # Better optimization
panic = "abort"     # Smaller binary
strip = true        # Strip symbols

[profile.bench]
inherits = "release"
opt-level = 3
lto = true
codegen-units = 1

[profile.native_release]
inherits = "release"
opt-level = 3
panic = "unwind"

[profile.python_release]
inherits = "release"
opt-level = 2
lto = "thin"

[profile.ffi_release]
inherits = "release"
opt-level = 2
lto = "thin"
panic = "unwind"
